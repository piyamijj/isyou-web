{
    "installCommand": "pnpm install --frozen-lockfile",
    "buildCommand": "pnpm --filter element-web build && cp apps/web/config.json apps/web/webapp/config.json",
    "outputDirectory": "apps/web/webapp",
    "framework": null
}